// import { getCachedStitches } from './cache';

export { createStitches } from './createStitches';
export { defaultThemeMap } from '@stitches/core';
export { convertResponsiveArrayForStyle, convertResponsiveArrayForVariant } from './utils';

// export const createTheme: Stitches.createTheme = (...args) => getCachedStitches().createTheme(...args);
// export const globalCss: Stitches.globalCss = (...args) => getCachedStitches().globalCss(...args);

// export const keyframes: Stitches.keyframes = (...args) => getCachedStitches().keyframes(...args);

// export const css: Stitches.keyframes = (...args) => getCachedStitches().css(...args);

// export const styled: Styled = (...args) => getCachedStitches().styled(...args);
