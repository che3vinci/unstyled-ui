// import { CreateStitches, createStitches } from '@stitches/core';

// let cachedStitches: ReturnType<CreateStitches>;

// export const getCachedStitches = () =>
//   cachedStitches || (cachedStitches = createStitches());
export {}
